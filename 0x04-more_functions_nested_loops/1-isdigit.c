#include <ctype.h>

/**
 * _isupper - checks for uppercase character
 *
 * @c: character check
 * Return: 1 if c uppercase
 *
 */

int _isdigit(int c)
{
	if (isdigit(c))
		return (1);
	else
		return (0);
}
