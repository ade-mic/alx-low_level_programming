#include <stdio.h>
/* prints Programing is funr
* to the standard output
*/
int main (void)
{
	/* string init */
  char mystr[] = "Programing is like building a multiligual puzzle" \n;
  puts(mystr);
  return 0;
}
