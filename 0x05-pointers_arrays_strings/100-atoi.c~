#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _atoi - convert string to integer
 *
 * @s: string to be converted
 *
 * Return: int
 */

int _atoi(char *s)
{
	int tmp = atoi(s);

	return (tmp);
}
