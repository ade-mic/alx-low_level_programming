#include "main.h"
#include <string.h>

/**
 * _strlen - it returns the length of a string
 *
 * @s: string to be length returned
 *
 * Return: the length of the string
 */

int _strlen(char *s)
{
	int length;

	length = s;
	return (strlen(length));
}
